export const S = {
  sectionLabel:
    "font-['Cinzel'] text-[9px] tracking-[0.35em] uppercase text-amber-800 mb-6",
  cardShell:
    "bg-amber-50/30 p-6 border border-stone-200/60 rounded-sm text-center flex flex-col justify-between",
  cardTitle:
    "font-['Cinzel'] text-[11px] font-bold mb-4 tracking-widest uppercase text-stone-800",
  cardBody:
    "font-['Cormorant_Garamond'] text-xl italic text-stone-600 mb-8 leading-relaxed flex-1",
  ghostBtn:
    "border border-stone-800 py-1 px-10 text-[10px] font-['Cinzel'] tracking-[0.3em] uppercase cursor-pointer transition-all",
} as const;
